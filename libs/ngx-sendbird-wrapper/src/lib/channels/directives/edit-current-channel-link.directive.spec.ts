import { EditCurrentChannelLinkDirective } from './edit-current-channel-link.directive';

describe('EditCurrentChannelLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new EditCurrentChannelLinkDirective();
    expect(directive).toBeTruthy();
  });
});
