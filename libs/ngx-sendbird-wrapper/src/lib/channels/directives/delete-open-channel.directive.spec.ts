import { DeleteOpenChannelDirective } from './delete-open-channel.directive';

describe('DeleteOpenChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteOpenChannelDirective();
    expect(directive).toBeTruthy();
  });
});
