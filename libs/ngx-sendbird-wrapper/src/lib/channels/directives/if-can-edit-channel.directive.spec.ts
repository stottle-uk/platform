import { IfCanEditChannelDirective } from './if-can-edit-channel.directive';

describe('IfCanEditChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new IfCanEditChannelDirective();
    expect(directive).toBeTruthy();
  });
});
