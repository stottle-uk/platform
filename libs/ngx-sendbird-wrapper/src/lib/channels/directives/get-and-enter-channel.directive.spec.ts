import { GetAndEnterChannelDirective } from './get-and-enter-channel.directive';

describe('GetAndEnterChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new GetAndEnterChannelDirective();
    expect(directive).toBeTruthy();
  });
});
