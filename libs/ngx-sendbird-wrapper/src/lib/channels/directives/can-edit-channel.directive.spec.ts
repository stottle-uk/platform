import { CanEditChannelDirective } from './can-edit-channel.directive';

describe('CanEditChannelDirective', () => {
  it('should create an instance', () => {
    const directive = new CanEditChannelDirective();
    expect(directive).toBeTruthy();
  });
});
