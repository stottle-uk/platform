import { UpdateMessageDirective } from './update-message.directive';

describe('UpdateMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateMessageDirective();
    expect(directive).toBeTruthy();
  });
});
