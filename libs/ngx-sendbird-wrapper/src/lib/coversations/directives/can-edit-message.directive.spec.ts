import { CanEditMessageDirective } from './can-edit-message.directive';

describe('CanEditMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new CanEditMessageDirective();
    expect(directive).toBeTruthy();
  });
});
