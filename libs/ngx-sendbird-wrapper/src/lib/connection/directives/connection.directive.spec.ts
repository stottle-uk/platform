import { ConnectionDirective } from './connection.directive';

describe('ConnectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectionDirective();
    expect(directive).toBeTruthy();
  });
});
